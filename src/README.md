# src/ — reserviert (noch leer)

Platzhalter für eine **spätere** Modularisierung des aktuell als Single-File-HTML
vorliegenden Tools. **In diesem ersten Schritt bewusst nicht begonnen** (kein Refactoring).

Geplante Aufteilung (Vorschlag, noch nicht umgesetzt):

- `src/data/` — eingebettete Daten ausgelagert (COMMUNES, MAP, MMAP, CITIES, LAKES,
  CANTONAL, MUNI, PENDABG, APPLIED, EXCLUDED, …)
- `src/styles/` — das aktuell inline vorliegende CSS

Maßgeblich bleibt bis dahin `legacy/regulierungs-kataster-original.html`.
